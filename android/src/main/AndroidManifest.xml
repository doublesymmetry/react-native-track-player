<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="guichaguri.trackplayer">

    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application>

        <receiver android:name=".logic.components.MediaReceiver">
            <intent-filter>
                <action android:name="android.intent.action.MEDIA_BUTTON" />
            </intent-filter>
        </receiver>

        <service android:name=".logic.PlayerService" android:enabled="true"/>
        <service android:name=".logic.PlayerTask" android:enabled="true"/>

    </application>

</manifest>
